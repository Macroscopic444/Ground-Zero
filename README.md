# Ground Zero SMP Website

Static website for Ground Zero SMP. Upload all files to a GitHub repository and enable GitHub Pages.

## Customize
- Edit page text in the `.html` files.
- Edit colors, fonts and layout in `style.css`.
- Put your own images in `assets/` and use them in CSS/HTML.
- Replace the Discord invite or server IP wherever needed.
